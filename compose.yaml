services:

  # ── Database ─────────────────────────────────────────────────────────────────
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-homepool}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD variable is required}
      POSTGRES_DB: ${POSTGRES_DB:-homepool}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-homepool}"]
      interval: 10s
      timeout: 5s
      retries: 5
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ── API FastAPI ──────────────────────────────────────────────────────────────
  api:
    # Prebuilt and published by CI. To compile it yourself instead, add the
    # build override: -f compose.yaml -f compose.build.yaml
    image: ghcr.io/johnschult/homepool-api:${HOMEPOOL_TAG:-latest}
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql+psycopg://${POSTGRES_USER:-homepool}:${POSTGRES_PASSWORD:?required}@db:5432/${POSTGRES_DB:-homepool}
      SESSION_SECRET: ${SESSION_SECRET:?SESSION_SECRET variable is required}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-http://localhost:8090}
      APP_BASE_URL: ${APP_BASE_URL:-http://localhost:8090}
      DEBUG: ${DEBUG:-false}
    depends_on:
      db:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ── Frontend React + Nginx ───────────────────────────────────────────────────
  web:
    image: ghcr.io/johnschult/homepool-web:${HOMEPOOL_TAG:-latest}
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-8090}:80"
    read_only: true
    tmpfs:
      - /var/cache/nginx
      - /var/run
      - /tmp
      - /etc/nginx/conf.d
    environment:
      BACKEND_URL: ${BACKEND_URL:-http://api:8003}
    depends_on:
      - api
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 128M
        reservations:
          cpus: "0.1"
          memory: 32M
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test: ["CMD", "nc", "-z", "127.0.0.1", "80"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  postgres_data:
